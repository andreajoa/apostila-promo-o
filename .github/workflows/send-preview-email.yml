name: Prévia de e-mail desativada

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Informação
        run: echo "O envio temporário de prévia foi concluído e desativado."
